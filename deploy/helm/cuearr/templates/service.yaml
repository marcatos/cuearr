apiVersion: v1
kind: Service
metadata:
  name: {{ include "cuearr.fullname" . }}
  labels:
    {{- include "cuearr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cuearr.selectorLabels" . | nindent 4 }}
